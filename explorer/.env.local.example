# Public endpoints (browser). Use the same-origin Nginx proxy path to avoid
# mixed-content (an http:// node URL from an https:// page is browser-blocked).
# The proxy upstream must point at a LIVE node — see docker/nginx/palimesh-rpc.conf.
# (The live canary host currently serves these paths under palimesh.io; the
#  palimesh.io cutover + rpc.palimesh.io are a separate DNS/Cloudflare task.)
NEXT_PUBLIC_RPC_URL=https://palimesh.io/api/testnet/rpc
NEXT_PUBLIC_WS_URL=wss://palimesh.io/api/testnet/ws

# Server-side SSR / Node (same chain as NEXT_PUBLIC). Point at a live node;
# v3 is the current primary (v4 159.198.36.3 / v5 159.198.36.25 are alternates).
PALI_RPC_URL=http://199.192.16.79:28780
